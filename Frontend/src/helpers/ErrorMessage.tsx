const ErrorMessage = () => {
    return (
        <div className="w-full h-full flex items-center justify-center flex-col">
            <p>An error has ocurry in recurry. Try refreshing. Contact us if the issues recurries</p>
        </div>
    )
}

export default ErrorMessage